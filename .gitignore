# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 產站輸出（GitHub Actions 會重新產，不進版控）
site/

# 系統 / 暫存
.DS_Store
*.output

# 注意：sve_meta.db 與 img_cache/ 故意「不」忽略 ——
# DB 含卡表/價格/歷史 meta；img_cache 是卡圖縮圖快取，
# 兩者都 commit，GitHub Actions 每日增量更新才不用重抓。
